# Contributing

1. Create a task in Aven and a branch `task/<REF>-…` (never commit on `master`).
2. Run `make test` and `make install`.
3. Keep Confluence credentials in `~/.config/md2c/md2c.conf` (see `md2c.conf.example`).
